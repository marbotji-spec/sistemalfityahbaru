import { createTeacher } from "./actions";
import { requireRole } from "@/lib/auth/authorization";

interface PageProps {
  searchParams: Promise<{ error?: string; success?: string }>;
}

export default async function TeachersPage({ searchParams }: PageProps) {
  const params = await searchParams;
  const { supabase } = await requireRole(["ADMIN", "KETUA_YAYASAN"]);
  const { data: teachers } = await supabase.from("profiles").select("id, full_name, email, phone, role, status, created_at").in("role", ["GURU", "KETUA_YAYASAN"]).order("created_at", { ascending: false });

  return (
    <main className="min-h-screen bg-[#f7fafc] px-5 py-8 lg:px-10"><div className="mx-auto max-w-7xl"><div className="mb-8"><p className="text-sm font-bold uppercase tracking-[0.18em] text-[#147fbd]">Administrasi</p><h1 className="mt-2 text-3xl font-bold text-[#112b45]">Data guru</h1><p className="mt-2 text-slate-500">Tambah dan kelola akun pengajar. Hanya Admin dan Ketua Yayasan yang dapat mengakses modul ini.</p></div>{params.error && <p className="mb-4 rounded-lg bg-[#fff1f2] p-3 text-sm font-medium text-[#c91d2e]">{params.error}</p>}{params.success && <p className="mb-4 rounded-lg bg-[#effaf5] p-3 text-sm font-medium text-[#087443]">{params.success}</p>}<div className="grid gap-8 xl:grid-cols-[360px_1fr]"><section className="h-fit rounded-xl border border-[#d8e3ec] bg-white p-5 shadow-sm"><h2 className="font-bold text-[#112b45]">Tambah akun guru</h2><form action={createTeacher} className="mt-5 space-y-4"><label className="block text-sm font-semibold text-[#112b45]">Nama lengkap<input name="full_name" required className="mt-1.5 w-full rounded-lg border border-[#c8d7e3] px-3 py-2 text-sm font-normal" /></label><label className="block text-sm font-semibold text-[#112b45]">Email login<input name="email" type="email" required className="mt-1.5 w-full rounded-lg border border-[#c8d7e3] px-3 py-2 text-sm font-normal" /></label><label className="block text-sm font-semibold text-[#112b45]">Nomor HP<input name="phone" required className="mt-1.5 w-full rounded-lg border border-[#c8d7e3] px-3 py-2 text-sm font-normal" /></label><label className="block text-sm font-semibold text-[#112b45]">Role<select name="role" className="mt-1.5 w-full rounded-lg border border-[#c8d7e3] px-3 py-2 text-sm font-normal"><option value="GURU">Guru</option><option value="KETUA_YAYASAN">Ketua Yayasan</option></select></label><button className="w-full rounded-lg bg-[#e52335] px-4 py-2.5 text-sm font-bold text-white hover:bg-[#c91d2e]">Buat akun guru</button></form></section><section className="rounded-xl border border-[#d8e3ec] bg-white shadow-sm"><div className="border-b border-[#d8e3ec] px-5 py-4"><h2 className="font-bold text-[#112b45]">Daftar guru</h2><p className="mt-1 text-sm text-slate-500">{teachers?.length ?? 0} akun terdaftar</p></div><div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="bg-[#f7fafc] text-xs uppercase tracking-wide text-slate-500"><tr><th className="px-5 py-3">Nama</th><th className="px-5 py-3">Kontak</th><th className="px-5 py-3">Role</th><th className="px-5 py-3">Status</th></tr></thead><tbody className="divide-y divide-[#d8e3ec]">{teachers?.map((teacher) => <tr key={teacher.id}><td className="px-5 py-4 font-semibold text-[#112b45]">{teacher.full_name}</td><td className="px-5 py-4 text-slate-600">{teacher.email ?? teacher.phone ?? "-"}</td><td className="px-5 py-4 text-slate-600">{teacher.role}</td><td className="px-5 py-4"><span className="rounded-full bg-[#effaf5] px-2.5 py-1 text-xs font-semibold text-[#087443]">{teacher.status}</span></td></tr>)}</tbody></table></div></section></div></div></main>
  );
}
